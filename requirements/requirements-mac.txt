# macOS / Apple Silicon (M1/M2/M3/M4) inference extras.
# Install AFTER `pip install -r requirements/requirements.txt`:
#   pip install -r requirements/requirements-mac.txt
#
# MLX is Apple's native ML framework — fast, low memory, Apple-Silicon only.
# Will not work on Intel Macs.

mlx>=0.20.0
mlx-lm>=0.20.0
accelerate>=0.30.0
